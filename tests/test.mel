fn print(var fmt: *char): void;
fn printInt(var num: u64): void;
fn printChar(var num: *char): void;
fn allocMem(var size: u64): *u64;
fn freeMem(var ptr: *u64): void;
fn getRand(var max: u64): u64;
fn printFmt(var fmt: *char, ...): u64;
fn getInput(var buf: *char, var size: int): *char;

struct position {
  var x: u64;
  var y: u64;
}

struct box {
  var width: u64;
  var height: u64;
  var pos: position;
}

fn test(var arr: *u64): void {
  printFmt("%ld\n", arr[0]);
  printFmt("%ld\n", arr[1]);
  printFmt("%ld\n", arr[2]);
  printFmt("%ld\n", arr[3]);
  printFmt("%ld\n", arr[4]);
}

fn main(): int {
  var arr: u64[5] = [1, 2, 3, 4, 5];
  test(arr);
  ret 0;
}