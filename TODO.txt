TODO:

struct.member[idx]
pointers to arrays.

Error the user if array dimension difference
Check if dereferencing pointer is the same type

multiple conditions (while (cond1 && cond2 && cond3 || cond4))

Implement continue and break keywords

Static and const objects.

Chars

For the future:
Implement casting!
switch statements
refactor probably
Assertions

========================
Code Gen TODO:

assignments
struct.member[idx]

for, if and while statements
Structure initialisation (maybe)


========================
Lib TODO:

Add HolyC-like casting (toU64(), toU32(), toPtr())